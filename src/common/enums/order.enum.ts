export enum OrderStatus {
    CREATED = 'created',
    CONFIRMED = 'confirmed',
    ASSIGNED = 'assigned',
    IN_PROGRESS = 'in_progress',
    SCHEDULED = 'scheduled',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
}
